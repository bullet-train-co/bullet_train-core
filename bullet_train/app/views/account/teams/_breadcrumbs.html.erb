<% team ||= @team %>
<% unless current_user.one_team? %>
  <%= render 'shared/breadcrumb', label: t('.label'), url: [:account, :teams], first: true %>
  <% if team&.persisted? %>
    <%= render 'shared/breadcrumb', label: team.name, url: [:account, team] %>
  <% end %>
<% end %>
<%= render 'shared/breadcrumbs/actions', only_for: 'teams' %>
<% if (action_name == 'show' && controller_name == 'teams') || current_user.one_team? %>
  <%= render 'shared/breadcrumb', label: 'Dashboard', url: [:account, team] %>
<% end %>
