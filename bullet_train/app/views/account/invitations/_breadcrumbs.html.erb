<% invitation ||= @invitation %>
<% team ||= @team || invitation&.team %>
<%= render 'account/teams/breadcrumbs', team: team %>
<%= render 'shared/breadcrumb', label: t('memberships.label'), url: [:account, team, :memberships] %>
<%= render 'shared/breadcrumb', label: t('.label'), url: [:account, team, :invitations] %>
<% if invitation&.persisted? %>
  <%= render 'shared/breadcrumb', label: invitation.label_string, url: [:account, invitation] %>
<% end %>
<%= render 'shared/breadcrumbs/actions', only_for: 'invitations' %>
