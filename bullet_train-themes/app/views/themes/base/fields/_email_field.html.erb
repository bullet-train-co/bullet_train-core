<%
form ||= current_fields_form
options ||= {}
other_options ||= {}
%>

<%= render 'shared/fields/field', form: form, method: method, helper: :email_field, options: options, other_options: other_options do |field| %>
  <% field.content_from partial, :label, :field, :error, :help, :after_help %>
<% end %>
