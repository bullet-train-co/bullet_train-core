<% access_token ||= @access_token %>
<% application ||= @application || access_token&.application %>
<%= render 'account/platform/applications/breadcrumbs', application: application %>
<%= render 'shared/breadcrumb', label: t('.label'), url: [:account, application, :access_tokens] %>
<% if access_token&.persisted? %>
  <%= render 'shared/breadcrumb', label: access_token.label_string, url: [:account, access_token] %>
<% end %>
<%= render 'shared/breadcrumbs/actions', only_for: 'platform/access_tokens' %>
